export const metadata = {
    title: "المتابعات",
    description: "أحدث مقالات الكتّاب الذين تتابعهم على إطناب.",
    robots: { index: false, follow: true },
};

export default function SubscriptionsLayout({ children }) {
    return children;
}
