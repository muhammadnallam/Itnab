PORT=
NODE_ENV=
DATABASE_URL=

BETTER_AUTH_SECRET=
BETTER_AUTH_URL=

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

IP_HASH_SALT=
MAXMIND_LICENSE_KEY=

# Email verification (optional). When enabled, a mail provider must be
# configured or verification links are dropped.
EMAIL_VERIFICATION_ENABLED=false
RESEND_API_KEY=
MAIL_FROM=

# Brute-force lockout tuning (defaults: 5 attempts / 15 min window).
AUTH_LOCKOUT_MAX_ATTEMPTS=5
AUTH_LOCKOUT_WINDOW_SECONDS=900

# Set to "true" only if Chromium's sandbox cannot run in the environment.
PUPPETEER_DISABLE_SANDBOX=false

CORS_ORIGIN=
TRUSTED_ORIGINS=